server:
  port: 3000

spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/customer
    username: customerdbadmin
    password: 04042003Murad@
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true
  main:
    web-application-type: servlet
